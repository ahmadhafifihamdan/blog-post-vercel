<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Blog</title>

    <style>
        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        body {
            margin: 0;
            padding: 32px 16px;
            background: #f4f6f8;
            color: #111;
        }

        .page {
            width: 100%;
            max-width: 760px;
            margin: 0 auto;
        }

        .header {
            margin-bottom: 16px;
        }

        .header h1 {
            margin: 0 0 6px;
            font-size: 24px;
        }

        .header p {
            margin: 0;
            color: #666;
            font-size: 14px;
        }

        .card {
            background: #fff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        }

        .form-grid {
            display: grid;
            gap: 16px;
        }

        label {
            display: block;
            margin-bottom: 6px;
            font-size: 14px;
            color: #333;
        }

        input[type="text"],
        textarea,
        input[type="file"] {
            width: 100%;
            font-size: 14px;
        }

        input[type="text"],
        textarea {
            border: 1px solid #ccc;
            border-radius: 6px;
            padding: 10px 12px;
            background: #fff;
        }

        textarea {
            resize: vertical;
            min-height: 180px;
            line-height: 1.4;
        }

        input[type="text"]:focus,
        textarea:focus {
            outline: none;
            border-color: #007bff;
        }

        .hint {
            margin-top: 6px;
            font-size: 12px;
            color: #777;
        }

        .actions {
            display: flex;
            gap: 12px;
            margin-top: 8px;
        }

        .btn {
            border: none;
            border-radius: 6px;
            padding: 12px 14px;
            font-size: 14px;
            cursor: pointer;
        }

        .btn-primary {
            background: #007bff;
            color: #fff;
            flex: 1;
        }

        .btn-primary:hover {
            background: #0069d9;
        }

        .btn-secondary {
            background: #e9ecef;
            color: #111;
            text-decoration: none;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            flex: 1;
        }

        .btn-secondary:hover {
            background: #dde2e6;
        }
        
        .alert {
          border-radius: 10px;
          padding: 10px 12px;
          font-size: 14px;
          margin-bottom: 12px;
        }

        .alert-error {
          background: #fdecec;
          border: 1px solid #f5c2c7;
          color: #842029;
        }
    </style>
</head>

<body>
    <div class="page">
        <div class="header">
            <h1>Create Blog Post</h1>
            <p>Title and post content are required. Image is optional.</p>
        </div>

        <div class="card">
            <% if (error) { %>
                <div class="alert alert-error">
                    <%= error %>
                </div>
            <% } %>
            <form method="POST" action="/blogs/create" enctype="multipart/form-data">
                <div class="form-grid">
                    <!-- Blog Title -->
                    <div>
                        <label for="title">Blog Title</label>
                        <input type="text" id="title" name="title" required placeholder="e.g. My first blog post" value="<%= title || '' %>" />
                    </div>

                    <!-- Blog Content -->
                    <div>
                        <label for="content">Post</label>
                        <textarea id="content" name="content" required placeholder="Write your post here..."><%= content || '' %></textarea>
                    </div>
                    
                    <!-- Image Header -->
                    <div>
                        <label for="imageHeader">Image Header (optional)</label>
                        <input type="text" id="imageHeader" name="imageHeader"
                        placeholder="Short caption for the image" value="<%= imageHeader || '' %>"/>
                    </div>
                    
                    <!-- Image Upload -->
                    <div>
                        <label for="image">Image (optional)</label>
                        <input type="file" id="image" name="image" accept="image/*" />
                        <div class="hint">Images only. (JPEG/PNG/WEBP recommended)</div>
                    </div>

                    <div class="actions">
                        <a class="btn btn-secondary" href="/blogs">Cancel</a>
                        <button class="btn btn-primary" type="submit">Create Blog</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>

</html>