<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Main</title>

    <style>
        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        body {
            margin: 0;
            background: #f4f6f8;
            color: #111;
        }

        .page {
            max-width: 900px;
            margin: 0 auto;
            padding: 24px 16px 40px;
        }

        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
        }

        .topbar h1 {
            margin: 0;
            font-size: 20px;
        }

        .topbar-actions {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .btn {
            border: none;
            border-radius: 8px;
            padding: 10px 12px;
            font-size: 14px;
            cursor: pointer;
        }

        .btn-primary {
            background: #007bff;
            color: #fff;
        }

        .btn-primary:hover {
            background: #0069d9;
        }

        .btn-secondary {
            background: #e9ecef;
            color: #111;
        }

        .btn-secondary:hover {
            background: #dde2e6;
        }

        .btn-danger {
            background: #dc3545;
            color: #fff;
        }

        .btn-danger:hover {
            background: #c82333;
        }

        .btn-liked {
            background: #e7f1ff;
            color: #0d6efd;
            border: 1px solid #cfe2ff;
        }

        .btn-liked:hover {
            background: #dbe9ff;
        }

        .card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            padding: 18px;
            text-align: left;
        }

        .blog-title {
            margin: 0 0 6px;
            font-size: 22px;
            line-height: 1.2;
        }

        .blog-meta {
            margin: 0 0 14px;
            color: #666;
            font-size: 13px;
        }

        .thumb-wrap {
            margin: 0 0 14px;
        }

        .thumb-link {
            display: inline-block;
            text-decoration: none;
        }

        .thumb {
            width: 100%;
            max-width: 520px;
            height: 260px;
            object-fit: cover;
            border-radius: 10px;
            border: 1px solid #eee;
            display: block;
            background: #f2f2f2;
            cursor: pointer;
        }

        .thumb:hover {
            opacity: 0.96;
        }

        @media (max-width: 520px) {
            .thumb {
                height: 200px;
            }
        }

        .thumb-caption {
            margin-top: 8px;
            font-size: 13px;
            color: #555;
            text-align: left;
        }

        .blog-content {
            margin: 0 0 14px;
            white-space: pre-wrap;
            line-height: 1.6;
            text-align: left;
        }

        .stats-row {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 12px;
            padding-top: 12px;
            border-top: 1px solid #eee;
            margin-top: 12px;
        }
        

        .likes-block {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .likes {
            font-size: 14px;
            color: #333;
        }

        .section-title {
            margin: 18px 0 10px;
            font-size: 16px;
        }

        .comments {
            list-style: none;
            padding: 0;
            margin: 0;
            display: grid;
            gap: 10px;
        }

        .comment-item {
            padding: 10px 12px;
            border: 1px solid #eee;
            border-radius: 10px;
            background: #fafafa;
            text-align: left;
        }

        .comment-text {
            margin: 0 0 6px;
            white-space: pre-wrap;
            text-align: left;
        }

        .comment-meta {
            font-size: 12px;
            color: #666;
            text-align: left;
        }

        .form-block {
            margin-top: 12px;
        }

        textarea {
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 10px 12px;
            font-size: 14px;
            min-height: 90px;
            resize: vertical;
        }

        textarea:focus {
            outline: none;
            border-color: #007bff;
        }

        .bottom-actions {
            margin-top: 12px;
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .next-form {
            align-self: flex-end;
        }

        .alert {
          border-radius: 10px;
          padding: 10px 12px;
          font-size: 14px;
          margin-bottom: 12px;
        }

        .alert-error {
          background: #fdecec;
          border: 1px solid #f5c2c7;
          color: #842029;
        }

        .alert-info {
          background: #eef6ff;
          border: 1px solid #cfe2ff;
          color: #084298;
        }

        .user-info {
            font-size: 13px;
            color: #555;
        }
    </style>
</head>

<body>
    <div class="page">

        <div class="topbar">
            <h1>Main Page</h1>
            <% if (user && user.email) { %>
                <div class="user-info">
                    Logged in as <strong>
                        <%= user.email %>
                    </strong>
                </div>
            <% } %>

            <div class="topbar-actions">
                <form method="GET" action="/blogs/create">
                    <button class="btn btn-primary" type="submit">Create Blog</button>
                </form>

                <form method="POST" action="/auth/logout">
                    <button class="btn btn-danger" type="submit">Logout</button>
                </form>
            </div>
        </div>

        <% if (blog) { %>
            <div class="card">
                <% if (typeof error !=="undefined" && error) { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                <% } %>
                <% if (notice) { %>
                    <div class="alert alert-info">
                        <%= notice %>
                    </div>
                <% } %>
                <article>
                    <h2 class="blog-title">
                        <%= blog.title %>
                    </h2>

                    <p class="blog-meta">By <%= blog.authorEmail %>
                    </p>

                    <% if (blog.imageUrl && String(blog.imageUrl).trim()) { %>
                        <div class="thumb-wrap">
                            <a class="thumb-link" href="<%= blog.imageUrl %>" target="_blank" rel="noopener noreferrer">
                                <img class="thumb" src="<%= blog.imageUrl %>" alt="Blog image" loading="lazy" />
                            </a>

                            <% if (blog.imageHeader && String(blog.imageHeader).trim()) { %>
                                <div class="thumb-caption">
                                    <%= blog.imageHeader %>
                                </div>
                                <% } %>
                        </div>
                        <% } %>

                            <p class="blog-content"><%= blog.content %></p>

                            <div class="stats-row">
                                <div class="likes-block">
                                    <div class="likes">Likes: <%= blog.likeCount %>
                                    </div>

                                    <form method="POST" action="/blogs/<%= blog.id %>/like">
                                        <button class="btn <%= hasLiked ? 'btn-liked' : 'btn-secondary' %>" type="submit">
                                            <%= hasLiked ? "Unlike" : "Like" %>
                                        </button>
                                    </form>
                                </div>

                                <form method="POST" action="/blogs/next" class="next-form">
                                    <button class="btn btn-secondary" type="submit">Next</button>
                                </form>
                            </div>
                </article>

                <h3 class="section-title">Comments</h3>

                <% if (comments.length> 0) { %>
                    <ul class="comments">
                        <% for (var i=0; i < comments.length; i++) { %>
                            <li class="comment-item">
                                <p class="comment-text"><%= comments[i].commentText %></p>
                                <div class="comment-meta">
                                    by <%= comments[i].userEmail %> • <%= comments[i].createdAtText %>
                                </div>
                            </li>
                            <% } %>
                    </ul>
                    <% } else { %>
                        <p class="blog-meta">No comments yet.</p>
                        <% } %>

                            <div class="form-block">
                                <form method="POST" action="/blogs/<%= blog.id %>/comment">
                                    <textarea name="commentText" required placeholder="Write a comment..."></textarea>

                                    <div class="bottom-actions">
                                        <button class="btn btn-primary" type="submit">Post Comment</button>
                                    </div>
                                </form>
                            </div>
            </div>
            <% } else { %>
                <div class="card">
                    <p>No blog posts available.</p>
                </div>
                <% } %>

    </div>
</body>

</html>